# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build e-commerce-be`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t e-commerce-be`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system e-commerce-be && \
          adduser --system -G e-commerce-be e-commerce-be

COPY dist/apps/e-commerce-be e-commerce-be/
RUN chown -R e-commerce-be:e-commerce-be .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix e-commerce-be --omit=dev -f install

CMD [ "node", "e-commerce-be" ]
